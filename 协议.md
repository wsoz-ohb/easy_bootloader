# Easy Bootloader 通信协议文档

## 1. 概述

本文档定义了 Easy Bootloader 通用框架的串口通信协议。升级数据由运行中的 APP 接收并暂存到外部 Flash，Bootloader 只在复位后执行校验、安装与回滚。

- **物理层**: UART, 115200-8-N-1, DMA + 空闲中断
- **字节序**: 大端序 (Big-Endian)

## 2. 通用帧格式

```
┌────────┬────────────┬────────┐
│ 包头   │  Payload   │ 包尾   │
│ 2B     │  nB        │ 2B     │
│0x55 0xAA│           │0x55 0x55│
└────────┴────────────┴────────┘
```

## 3. 固件下载协议

### 3.1 数据帧格式

用于传输固件数据，最大 1024 字节/包。

```
┌────────┬────────────┬────────────┬──────────┬──────────┬────────┐
│ 包头   │ 剩余字节   │ 本包长度   │ 数据     │ 校验和   │ 包尾   │
│ 2B     │ 3B         │ 2B         │ ≤1013B   │ 2B       │ 2B     │
│0x55 0xAA│            │            │          │          │0x55 0x55│
└────────┴────────────┴────────────┴──────────┴──────────┴────────┘
```

| 字段 | 长度 | 说明 |
|------|------|------|
| 包头 | 2B | 固定 `0x55 0xAA` |
| 剩余字节 | 3B | 当前帧之后仍需传输的字节数，大端序，最后一包为 0 |
| 本包长度 | 2B | 数据区字节数，大端序，最大 1013 |
| 数据区 | ≤1013B | 固件原始字节流 |
| 校验和 | 2B | (本包长度 + 数据区) 逐字节累加，大端序 |
| 包尾 | 2B | 固定 `0x55 0x55` |

### 3.2 ACK 应答帧

APP 每成功写入一帧到外部 Flash 并完成回读校验后回传：

```
0x55 0xAA 0xFF 0xFE 0x55 0x55 (6字节)
```

### 3.3 完成帧格式

当所有数据帧传输完成（最后一包 `剩余字节=0`）后，上位机需发送完成帧告知版本信息：

```
┌────────┬────────────┬────────────┬────────┬────────┐
│ 包头   │ 版本号     │ 更新时间   │ 命令码 │ 包尾   │
│ 2B     │ 4B         │ 4B         │ 2B     │ 2B     │
│0x55 0xAA│            │            │0xFF 0xFD│0x55 0x55│
└────────┴────────────┴────────────┴────────┴────────┘
```

| 字段 | 偏移 | 长度 | 说明 |
|------|------|------|------|
| 包头 | 0 | 2B | 固定 `0x55 0xAA` |
| 版本号 | 2 | 4B | uint32_t, 大端序 |
| 更新时间 | 6 | 4B | uint32_t, 格式 `0xYYYYMMDD` |
| 命令码 | 10 | 2B | 固定 `0xFF 0xFD` |
| 包尾 | 12 | 2B | 固定 `0x55 0x55` |

**示例**: 版本号 `0x00000002`, 更新时间 `2025-12-15`
```
55 AA 00 00 00 02 20 25 12 15 FF FD 55 55
```

**响应**: APP 写入镜像头并提交 BCB `UPDATE_READY` 记录后回传 ACK，然后复位。Bootloader 随后把待升级镜像安装到内部 APP 区。

## 4. APP 运行时协议

### 4.1 查询版本命令

**请求** (6字节):
```
0x55 0xAA 0xFF 0xDD 0x55 0x55
```

**响应**: 串口返回 `version:xx\r\n`

### 4.2 查询更新时间命令

**请求** (6字节):
```
0x55 0xAA 0xFF 0xCC 0x55 0x55
```

**响应**: 串口返回 `YYYY-MM-DD\r\n` (如 `2025-12-01\r\n`)

### 4.3 启动升级命令

**请求** (6字节):
```
0x55 0xAA 0xFF 0xEE 0x55 0x55
```

**响应**: APP 根据 BCB 选择非活动外部槽，建立下载会话并发送 ACK `0x55 0xAA 0xFF 0xFE 0x55 0x55`。此时不复位。

> **注意**: 启动升级命令不携带版本号和日期；这些信息由上位机在下载完成后通过**完成帧**发送给 APP。

## 5. BCB 控制区与镜像槽

BCB（Boot Control Block）位于内部 Flash Sector 3，默认地址 `0x0800C000`、大小 16KB。它不是单一 Flag，而是由多条 64B 追加记录组成，用于保存当前确认槽、待安装槽、版本、镜像大小、CRC、启动次数和错误状态。

外部 W25Q128 默认布局：

| 区域 | 偏移 | 大小 | 说明 |
|------|------|------|------|
| Slot A | `0x00200000` | 2MB | 镜像头 64B + 固件数据 |
| Slot B | `0x00400000` | 1MB | 镜像头 64B + 固件数据 |

APP 总是下载到 `confirmed_slot` 的另一槽；首次没有已确认外部槽时下载到 A，Bootloader 会先把当前内部 APP 备份到 B，再安装 A。

候选槽在新 APP 确认前不会覆盖确认槽。默认最多试运行 2 次；候选 APP 未确认即反复复位、内部镜像校验失败或向量表无效时，Bootloader 会从 `confirmed_slot` 恢复旧 APP。

## 6. 升级流程

```
┌─────────┐     ┌─────────┐     ┌─────────────┐
│ 上位机  │     │   APP   │     │ Bootloader  │
└────┬────┘     └────┬────┘     └──────┬──────┘
     │               │                  │
     │ 启动升级命令  │                  │
     │──────────────>│                  │
     │               │                  │
     │     ACK       │                  │
     │<──────────────│                  │
     │               │                  │
     │ 固件数据包  │                  │
     │────────────>│                  │
     │     ACK     │                  │
     │<────────────│                  │
     │     ...     │                  │
     │ 最后一包    │                  │
     │────────────>│                  │
     │     ACK     │                  │
     │<────────────│                  │
     │ 完成帧      │                  │
     │────────────>│                  │
     │             │ 回读CRC并提交BCB │
     │     ACK     │                  │
     │<────────────│                  │
     │             │ 系统复位         │
     │             └─────────────────>│
     │                                │ 安装待升级镜像
     │                                │ 进入试运行APP
     │                                   │
```

首次升级时，Bootloader 在安装 A 槽前会将当前内部 APP 备份到 B 槽。后续升级按确认槽交替下载：确认 A 时下载 B，确认 B 时下载 A。只有候选 APP 确认后才切换确认槽。

## 7. 命令码汇总

| 命令 | 特征字节 | 帧长度 | 方向 |
|------|----------|--------|------|
| 查询版本 | `0xFF 0xDD` | 6B | 上位机 → APP |
| 查询更新时间 | `0xFF 0xCC` | 6B | 上位机 → APP |
| 启动升级 | `0xFF 0xEE` | 6B | 上位机 → APP |
| ACK 应答 | `0xFF 0xFE` | 6B | 设备 → 上位机 |
| 完成帧 | `0xFF 0xFD` | 14B | 上位机 → APP |
